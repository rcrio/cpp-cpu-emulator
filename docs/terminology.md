# Terminology

## n-bit architecture

The **n** describes the size of the CPU's general-purpose registers.

A higher **n** allows the CPU to:

- More easily support larger memory addresses.
- Directly operate on larger numbers without splitting them into smaller parts.
